spring:
    application:
        name: suns-web-system

    thymeleaf:
        prefi: classpath:/templates/
        suffix: .html
        encoding: UTF-8
        cache: false

server:
    port: 8080

